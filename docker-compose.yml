services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
      DB_PATH: skypulse.db
      ANALYTICS_API_KEY: dummy
      ANALYTICS_ENDPOINT: https://httpbin.org/post
